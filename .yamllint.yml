---
extends: default
rules:
  line-length:
    max: 120
  document-start: enable
  indentation:
    spaces: 2
  truthy:
    allowed-values: ["true", "false", "on", "off"]
  comments:
    min-spaces-from-content: 1
