<!-- RM0503 page 571 -->

### 23.3.4 External trigger input

The timer features an external trigger input ETR. It can be used as:

- external clock (external clock mode 2, see *Section 23.3.5*)
- trigger for the slave mode (see *Section 23.3.26*)
- PWM reset input for cycle-by-cycle current regulation (see *Section 23.3.7*)

*Figure 142* below describes the ETR input conditioning. The input polarity is defined with the
ETP bit in TIMxSMCR register. The trigger can be prescaled with the divider programmed
by the ETPS[1:0] bitfield and digitally filtered with the ETF[3:0] bitfield.

**Figure 142. External trigger input block**

ETR input
ETR

ETRP

To the Output mode controller
To the CK_PSC circuitry
To the Slave mode controller


Divider
/1, /2, /4, /8

Filter
downcounter

fDTS


ETP
TIMx_SMCR

ETPS[1:0]
TIMx_SMCR

ETF[3:0]
TIMx_SMCR

MS34403V2

The ETR input comes from multiple sources: input pins (default configuration), comparator
outputs and analog watchdogs. The selection is done with the ETRSEL[3:0] bitfield.

**Figure 143. TIM1 ETR input circuitry**

TIM1_AF1[17:14]

ETR inputs from AF controller
COMP1 output
COMP2 output
ADC1 AWD1
ADC1 AWD2
ADC1 AWD3

ETR input

NC

MSv71438V1
