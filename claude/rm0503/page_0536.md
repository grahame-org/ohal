<!-- RM0503 page 536 -->

**Figure 118. 128-bit block construction with respect to data swap**

_Note: The data in AES key registers (AES_KEYRx) and initialization registers (AES \_IVRx) are not sensitive to the swap mode selection._

#### Data padding

_Figure 118_ also gives an example of memory data block padding with zeros such that the
zeroed bits after the data swap form a contiguous zone at the MSB end of the AES core
input buffer. The example shows the padding of an input data block containing:

- 48 message bits, with DATATYPE[1:0] = 01
- 56 message bits, with DATATYPE[1:0] = 10
- 34 message bits, with DATATYPE[1:0] = 11
