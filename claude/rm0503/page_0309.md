<!-- RM0503 page 309 -->

*Note:*
*A detected configurable event interrupt pending request can be cleared by the CPU. The*
*system cannot enter low-power modes as long as an interrupt pending request is active.*

### 12.3.2 EXTI direct event input wake-up

*Figure 28* is a detailed representation of the logic associated with direct event inputs waking
up the system.

The direct events do not have an associated EXTI interrupt. The EXTI only wakes up the
system and CPU sub-system clocks and may generate a CPU wake-up event. The
peripheral synchronous interrupt, associated with the direct wake-up event wakes up the
CPU.

The EXTI direct event is able to generate a CPU event. This CPU event wakes up the CPU.
The CPU event may occur before the interrupt flag of the associated peripheral is set.

**Figure 28. Direct event trigger logic CPU wake-up**

AHB
interface

Peripheral interface

CPU
interrupt
mask
register

CPU event
mask
register

hclk

Same circuit for configurable
and direct events

EVG

hclk

ck_fclk_c

hclk

c_evt_rst

Delay

CPU rising
edge detect
pulse
generator

CPU Event(x)

|  | c evt exti _ _ |
| --- | --- |

Rising edge
detect

c_event

Other CPU Events(x,y)

rst

Asynchronous
rising edge
detect circuit

Direct
event
input(x)

hclk

Synch

c_wakeup

CPU Wake-up(x)

rst

Falling edge
detect
pulse
generator

sys_wakeup
Other CPU
Wake-ups
Other Wake-ups

Wake-up(x)

EXTI

hclk

MSv71279V1

### 12.3.3 EXTI mux

The EXTI mux allows selecting GPIOs as interrupts and wake-up. The GPIOs are
connected via 16 EXTI mux lines to the first 16 EXTI events as configurable event. The
selection of GPIO port as EXTI mux output is controlled through the *EXTI external interrupt*
*selection register x (EXTI_EXTICRx)* register.
