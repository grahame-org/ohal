<!-- RM0503 page 1065 -->

#### Determining the maximum USART baud rate that enables to correctly wake
up the microcontroller from low-power mode

The maximum baud rate that enables to correctly wake up the microcontroller from low-
power mode depends on the wake-up time parameter (refer to the device datasheet) and on
the USART receiver tolerance (see *Section 34.5.9: Tolerance of the USART receiver to*
*clock deviation*).

Let us take the example of OVER8 = 0, M bits = 01, ONEBIT = 0 and BRR [3:0] = 0000.

In these conditions, according to *Table 204: Tolerance of the USART receiver when BRR*
*[3:0] = 0000*, the USART receiver tolerance equals 3.41%.

DTRA + DQUANT + DREC + DTCL + DWU < USART receiver tolerance

DWUmax = tWUUSART/ (11 x Tbitmin)

Tbitmin = tWUUSART/ (11 x DWUmax)

where tWUUSART is the wake-up time from low-power mode.

If we consider the ideal case where DTRA, DQUANT, DREC, and DTCL parameters are at
0%, the maximum value of DWU is 3.41%. In fact, we need to consider at least the
usart_ker_ck inaccuracy (DREC).

For example, if HSI is used as usart_ker_ck, and the HSI inaccuracy is of 1%, then we
obtain:

tWUUSART = 3 μs (values provided only as examples; for correct values, refer to the
device datasheet).

DWUmax = USART receiver tolerance - DREC = 3.41% - 1% = 2.41%

Tbitmin = 3 μs/ (11 x 2.41%) = 11.32 μs.

As a result, the maximum baud rate enables to wake up correctly from low-power mode
is: 1/11.32 μs = 88.36 kbauds.

### 34.6 USART in low-power modes

**Table 207. Effect of low-power modes on the USART**

| Mode | Description |
| --- | --- |
| Sleep | No effect. USART interrupts cause the device to exit Sleep mode. |
| Stop(1) | The content of the USART registers is kept. The USART is able to wake up the microcontroller from Stop mode when the USART is clocked by an oscillator available in Stop mode. |
| Standby | The USART peripheral is powered down and must be reinitialized after exiting Standby mode. |

1. Refer to *Section 34.4: USART implementation* to know if the wake-up from Stop mode is supported for a
   given peripheral instance. If an instance is not functional in a given Stop mode, it must be disabled before
   entering this Stop mode.

### 34.7 USART interrupts

Refer to *Table 208* for a detailed description of all USART interrupt requests.
