<!-- RM0503 page 1133 -->

**Figure 366. Wake-up event verified (wake-up event = address match,**
**FIFO disabled)**

Figure 367 shows the behavior when the wake-up event is not verified.

**Figure 367. Wake-up event not verified (wake-up event = address match,**
**FIFO disabled)**

Note:
The above figures are valid when address match or any received frame is used as wake-up
event. In the case the wake-up event is the start bit detection, the LPUART sends the wake-
up event to the MCU at the end of the start bit.
